#include <stdio.h>

/**
 * main-prints the words with proper grammar, but the outcome is a piece of
 * art,
 * Return: 0 if exited properly, no zero other wise
 */
int main(void)
{
     printf("with proper grammar, but the outcome is a piece of art,\n");
     return (0);
}
