#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the ineger to check
 * Return: the absolute value of integer
 */

int -abs(int n)

{

	if (n >= 0)
	{

		return (n);

	}
	return (-n);

}
